/**
 * Pipelines
 */

export default class WarlockPipeline {
  constructor ( name ) {
    this._name = name;
  }

  options () {}

  append () {}
  prepend () {}
  before () {}
  after () {}

  run () {
    // handle input
    // run through the pipeline
  }
}

